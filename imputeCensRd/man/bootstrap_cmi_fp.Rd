% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap_cmi_fp.R
\name{bootstrap_cmi_fp}
\alias{bootstrap_cmi_fp}
\title{Bootstrap fully parametric conditional mean imputation (CMI) for a censored predictor}
\usage{
bootstrap_cmi_fp(
  analysis_model,
  W,
  Delta,
  Z,
  data,
  fit = NULL,
  dist = "weibull",
  trapezoidal_rule = FALSE,
  B = 1000
)
}
\arguments{
\item{analysis_model}{A formula input for the final analysis model of interest. Note that}

\item{W}{Column name of observed predictor values (including censored opens).}

\item{Delta}{Column name of censoring indicators. Note that \code{Delta = 0} is interpreted as a censored observation.}

\item{Z}{Column name of additional fully observed covariates.}

\item{data}{Dataframe or named matrix containing columns \code{W}, \code{Delta}, and \code{Z}.}

\item{fit}{A \code{survreg} imputation model object modeling \code{W} on \code{Z}. If \code{fit = NULL} (default), the AFT model with only main effects for \code{Z} and assuming a Weibull distribution is fit internally and used.}

\item{dist}{(Optional) Assumed distribution for \code{W} in the AFT model, passed to \code{survival::survreg()}. Default is \code{"weibull"}.}

\item{trapezoidal_rule}{A logical input for whether the trapezoidal rule should be used to approximate the integral in the imputed values. Default is \code{FALSE}.}

\item{B}{Number of bootstrap resampling replicates. Default is \code{B = 1000}.}
}
\value{
\item{imputed_data}{A copy of \code{data} with added column \code{imp} containing the imputed values.}
\item{code}{Indicator of algorithm status (\code{TRUE} or \code{FALSE}).}
}
\description{
Bootstrap fully parametric conditional mean imputation (CMI) for a censored predictor using an accelerated failure-time (AFT) model to estimate conditional survival.
}
